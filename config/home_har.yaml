# HomeHAR CSI dataset — target (roles swapped vs home_occupancy.yaml)
data:
  dataset: home_har
  public_dataset: home_occupancy
  n_classes: 7
  n_samples_per_class: 30
  window_size: 1500
  n_stft_bins: 8          # STFT freq bins retained; n_features = 4 + n_stft_bins = 12
