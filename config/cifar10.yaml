# CIFAR-10 benchmark — MNIST images resized to (32,32,3) used as public KD pool
data:
  dataset: cifar10
  public_dataset: mnist
  n_classes: 10
  n_samples_per_class: 30
  img_size: 32
  in_channels: 3

federated:
  n_rounds: 40
  local_epochs: 3
  kd_epochs: 2
  n_alignment: 10000
