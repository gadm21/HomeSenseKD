# MNIST benchmark — CIFAR-10 images resized to (28,28,1) used as public KD pool
data:
  dataset: mnist
  public_dataset: cifar10
  n_classes: 10
  n_samples_per_class: 100
  img_size: 28
  in_channels: 1

federated:
  n_rounds: 50
  local_epochs: 5
  kd_epochs: 3
  n_alignment: 5000
